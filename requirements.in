anthropic>=0.40.0,<1
openai>=2.53.0,<3
python-dotenv>=1.0.1,<2
jsonschema>=4.20.0,<5
keyring>=25.0.0,<26
# jaraco-context exposes this dependency only on Python 3.11; keep it explicit
# so universal hash locks remain complete when consumed by pip.
backports.tarfile>=1.2,<2; python_version < "3.12"
